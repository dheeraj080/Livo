package com.lv.notes.domain.entity;

public enum Provider {
    LOCAL,    // Registered via your app's signup form
    GOOGLE,   // Logged in via Google OAuth2
    GITHUB,   // Logged in via GitHub OAuth2
    FACEBOOK  // Logged in via Facebook OAuth2
}
